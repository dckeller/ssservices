<div class="new-user-intro">
  <h2>Why Sign Up?</h2>
  <h3>Our independent insepctor portal is meant to better communicate your availability that you would like to use for extra billable time & connect with potential clients along with creating a network with other independent inspectors.</h3>
</div>

<div class="new-user-form">

  <%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %>
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>
  <br>
  <div class="seperator"></div>
  <br>
  <div class="field">
    <%= f.text_field :bus_name, :label => "Business Name", autofocus: true, autocomplete: "bus_name" %>
  </div>

  <div class="field">
    <%= f.text_field :first_name, autofocus: true, autocomplete: "first_name" %>
  </div>

  <div class="field">
    <%= f.text_field :last_name, autofocus: true, autocomplete: "last_name" %>
  </div>
  <br>
  <div class="seperator"></div>
  <br>
  
  <div class="field">
    <%= f.file_field :avatar, :label => "Attach Business Logo", as: :file %>
  </div>

  <div class="actions">
    <%= f.submit "Sign Up", class: "btn btn-default" %>
  </div>
  <% end %>
</div>