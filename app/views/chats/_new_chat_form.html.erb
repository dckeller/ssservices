<%= form_for @chat, url: slot_chats_path(@slot[:id]), method: :post do |f| %>
	<%= f.hidden_field :slot_creator_id, value: @slot.slot_creator_id %>
	<%= f.hidden_field :worker_id, value: current_user.id %>
	<%= f.submit "Chat" %>
<% end %>


